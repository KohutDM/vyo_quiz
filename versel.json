{
  "builds": [{
    "src": "quiz_game/wsgi.py",
    "use": "@versel/python",
    "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
  }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "quiz-game/wsgi.py"
    }
  ]
}
